const URL = 'http://localhost:4000/';

export const notification = {
    url: URL.concat('notification/')
}
export const chat = {
    url: URL.concat('chat/')
}
export const user = {
    url: URL.concat('user/')
}
export const conversation = {
    url: URL.concat('conversation/')
}